import { updateCart } from './CartSlice';

export const changeCartAction =
  (productPropertiesForCart, cartItem) => async (dispatch) => {
    // return dispatch(updateCart(productPropertiesForCart));
    // const newCart = cartItem.push(productPropertiesForCart);
    // return dispatch(updateCart(cartItem.push(productPropertiesForCart)));
  };
